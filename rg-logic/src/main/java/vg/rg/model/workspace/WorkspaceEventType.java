package vg.rg.model.workspace;

/**
 * The persisted event categories. Values are stored by ordinal, so append new values only and never
 * reorder the existing declaration.
 */
public enum WorkspaceEventType {
    PADEL,
    TENNIS
}
