project_version=0.1.3

# The Java version, in one place. Pinned as a toolchain in build.gradle, so it decides which javac
# compiles and which JVM runs the tests -- not merely which language level is accepted.
java_version=21

spring_boot_version=4.1.1
spring_dependency_management_version=1.1.7
mapstruct_version=1.6.3
vaadin_version=25.2.2

spring_cloud_oci_version=2.0.3

vg_lib_version=0.0.1
vg_unique_id_version=0.0.2
vg_identity_version=0.0.3
